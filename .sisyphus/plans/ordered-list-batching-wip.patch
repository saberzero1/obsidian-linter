diff --git a/__tests__/batched-mdast-edits.test.ts b/__tests__/batched-mdast-edits.test.ts
index 30e2c91..3c71ffb 100644
--- a/__tests__/batched-mdast-edits.test.ts
+++ b/__tests__/batched-mdast-edits.test.ts
@@ -1,8 +1,83 @@
-import {removeSpacesInLinkText, UnorderedListItemStyles, updateUnorderedListItemIndicators} from '../src/utils/mdast';
+import OrderedListStyle from '../src/rules/ordered-list-style';
+import {OrderListItemEndOfIndicatorStyles, OrderListItemStyles, removeSpacesInLinkText, UnorderedListItemStyles, updateUnorderedListItemIndicators} from '../src/utils/mdast';
 import {ProtectedRanges} from '../src/utils/protected-ranges';
 import * as strings from '../src/utils/strings';
 
 describe('batched mdast edits', () => {
+  it.each([
+    {
+      name: 'separate top-level lists',
+      before: '3. first\n8. second\n\nParagraph\n\n7. third\n9. fourth',
+      after: '1. first\n2. second\n\nParagraph\n\n1. third\n2. fourth',
+    },
+    {
+      name: 'top-level lists separated by protected content',
+      before: '3. first\n8. second\n<!-- linter-disable -->\n9. hidden\n<!-- linter-enable -->\n7. third\n9. fourth',
+      after: '1. first\n2. second\n<!-- linter-disable -->\n9. hidden\n<!-- linter-enable -->\n1. third\n2. fourth',
+    },
+    {
+      name: 'tabs and blockquote prefixes',
+      before: '> 1. first\n> \t1. child\n> \t1. sibling\n> 1. second\n> \t1. next child\n> \t1. next sibling',
+      after: '> 1. first\n> \t1. child\n> \t2. sibling\n> 2. second\n> \t1. next child\n> \t2. next sibling',
+    },
+    {
+      name: 'an unordered marker interrupting ordered descendants',
+      before: '- parent\n  1. first\n  1. second\n  - interruption\n  1. third\n  1. fourth',
+      after: '- parent\n  1. first\n  2. second\n  - interruption\n  3. third\n  4. fourth',
+    },
+    {
+      name: 'an unordered parent resetting ordered descendants',
+      before: '- parent\n  1. first\n  1. second\n- interruption\n  1. third\n  1. fourth',
+      after: '- parent\n  1. first\n  2. second\n- interruption\n  1. third\n  2. fourth',
+    },
+    {
+      name: 'protected code indicators neither incrementing nor resetting counters',
+      before: '1. parent\n   1. first\n   ```\n   90. code\n   - code\n   ```\n   1. second\n1. sibling',
+      after: '1. parent\n   1. first\n   ```\n   90. code\n   - code\n   ```\n   2. second\n2. sibling',
+    },
+  ])('preserves ordered-list decisions for $name', ({before, after}) => {
+    expect(OrderedListStyle.getRule().apply(before)).toBe(after);
+  });
+
+  it.each([
+    {
+      numberStyle: OrderListItemStyles.Ascending,
+      after: '3. parent\n   4. first\n   5. second\n4. sibling\n   7. third\n   8. fourth',
+    },
+    {
+      numberStyle: OrderListItemStyles.Lazy,
+      after: '3. parent\n   4. first\n   4. second\n3. sibling\n   7. third\n   7. fourth',
+    },
+  ])('preserves nested sibling starts with $numberStyle numbering', ({numberStyle, after}) => {
+    const before = '3. parent\n   4. first\n   8. second\n8. sibling\n   7. third\n   9. fourth';
+    expect(OrderedListStyle.getRule().apply(before, {numberStyle, preserveStart: true})).toBe(after);
+  });
+
+  it('batches only original indicator ranges when nested numbering grows and shrinks', () => {
+    const before = '9. parent\n   9. first\n   1. second\n99. sibling\n   4. third\n   99. fourth';
+    const after = '9) parent\n   9) first\n   10) second\n10) sibling\n   4) third\n   5) fourth';
+    const replaceTextRanges = jest.spyOn(strings, 'replaceTextRanges');
+    try {
+      expect(OrderedListStyle.getRule().apply(before, {
+        preserveStart: true,
+        listEndStyle: OrderListItemEndOfIndicatorStyles.Parenthesis,
+      })).toBe(after);
+      expect(replaceTextRanges).toHaveBeenCalledTimes(1);
+      const replacements = replaceTextRanges.mock.calls[0][1];
+      expect(replacements).toHaveLength(6);
+      for (const replacement of replacements) {
+        expect(before.substring(replacement.startIndex, replacement.endIndex)).toMatch(/^\d+\.$/);
+        expect(replacement.value).toMatch(/^\d+\)$/);
+      }
+      for (let index = 1; index < replacements.length; index++) {
+        expect(replacements[index - 1].startIndex).toBeLessThan(replacements[index].startIndex);
+        expect(replacements[index - 1].endIndex).toBeLessThanOrEqual(replacements[index].startIndex);
+      }
+    } finally {
+      replaceTextRanges.mockRestore();
+    }
+  });
+
   it.each([
     {
       before: '- outer\n  + inner\n    - deepest\n- sibling',
diff --git a/src/utils/mdast.ts b/src/utils/mdast.ts
index 5d34182..8e8c024 100644
--- a/src/utils/mdast.ts
+++ b/src/utils/mdast.ts
@@ -920,27 +920,27 @@ export function updateOrderedListItemIndicators(text: string, orderedListStyle:
     return text;
   }
 
-  const listItemRegex = /^(( |\t|> )*)((\d+(\.|\)))|[-*+])([^\n]*)$/gm;
-  const protectedIndicatorLines = new Set<number>();
-  let sourceLine = 0;
-  let previousMatchOffset = 0;
-  // Nested lists are rewritten before their parents and can change indicator widths. Record
-  // eligibility against the original text by line, since renumbering never changes newlines.
-  for (const match of text.matchAll(listItemRegex)) {
-    sourceLine += countInstances(text.substring(previousMatchOffset, match.index), '\n');
-    previousMatchOffset = match.index;
-    const indicatorStart = match.index + match[1].length;
-    if (protectedRanges.isProtected(indicatorStart, indicatorStart + match[3].length)) {
-      protectedIndicatorLines.add(sourceLine);
+  // The outermost list recomputes every descendant's indicator with its own level map.
+  // Visit only those disjoint ranges, keeping all indicator offsets relative to the original text.
+  positions.sort((a, b) => a.start.offset - b.start.offset || b.end.offset - a.end.offset);
+  const outermostPositions: Position[] = [];
+  for (const position of positions) {
+    const previous = outermostPositions[outermostPositions.length - 1];
+    if (previous && position.end.offset <= previous.end.offset &&
+        (position.start.offset > previous.start.offset || position.end.offset < previous.end.offset)) {
+      continue;
     }
+    outermostPositions.push(position);
   }
 
-  for (const position of positions) {
+  const listItemRegex = /^(( |\t|> )*)((\d+(\.|\)))|[-*+])([^\n]*)$/gm;
+  const replacements: textReplacement[] = [];
+  for (const position of outermostPositions) {
     let start = position.start.offset;
     while (start > 0 && text.charAt(start - 1) !== '\n') {
       start--;
     }
-    let listText = text.substring(start, position.end.offset);
+    const listText = text.substring(start, position.end.offset);
 
     const getListItemLevel = function(preListItemIndicatorContent: string): number {
       const lastBlockQuoteIndicator = preListItemIndicatorContent.lastIndexOf('> ');
@@ -962,14 +962,15 @@ export function updateOrderedListItemIndicators(text: string, orderedListStyle:
     };
 
     let lastItemListIndicatorLevel = -1;
-    let currentLine = countInstances(text.substring(0, start), '\n');
-    let previousListMatchOffset = 0;
-    listText = listText.replace(listItemRegex, (listItem: string, $1: string = '', _$2: string, $3: string, _$4: string, _$5: string, $6: string, offset: number) => {
-      currentLine += countInstances(listText.substring(previousListMatchOffset, offset), '\n');
-      previousListMatchOffset = offset;
+    for (const match of listText.matchAll(listItemRegex)) {
+      const $1 = match[1];
+      const $3 = match[3];
+      const _$4 = match[4];
+      const indicatorStart = start + match.index + $1.length;
+      const indicatorEnd = indicatorStart + $3.length;
       // Masked indicators neither changed nor participated in level/counter tracking.
-      if (protectedIndicatorLines.has(currentLine)) {
-        return listItem;
+      if (protectedRanges.isProtected(indicatorStart, indicatorEnd)) {
+        continue;
       }
 
       // _$4 is the indicator with its terminator attached (`1.` or `1)`), so it has to be
@@ -981,7 +982,7 @@ export function updateOrderedListItemIndicators(text: string, orderedListStyle:
         const highestCurrentValue = listItemIndicatorLevel > lastItemListIndicatorLevel ? listItemIndicatorLevel: lastItemListIndicatorLevel;
         removeListItemsItemIndicatorInfo(listItemIndicatorLevel, highestCurrentValue);
 
-        return listItem; // skip to the next item if the current item is not an ordered list item
+        continue; // skip to the next item if the current item is not an ordered list item
       }
 
       if (preListIndicatorLevelsToIndicatorNumber.has(listItemIndicatorLevel)) {
@@ -1002,13 +1003,11 @@ export function updateOrderedListItemIndicators(text: string, orderedListStyle:
 
       lastItemListIndicatorLevel = listItemIndicatorLevel;
 
-      return `${$1}${listItemIndicatorNumber}${orderedListEndStyle}${$6}`;
-    });
-
-    text = replaceTextBetweenStartAndEndWithNewValue(text, start, position.end.offset, listText);
+      replacements.push({startIndex: indicatorStart, endIndex: indicatorEnd, value: `${listItemIndicatorNumber}${orderedListEndStyle}`});
+    }
   }
 
-  return text;
+  return applyNonOverlappingReplacements(text, replacements);
 }
 
 export function updateUnorderedListItemIndicators(text: string, unorderedListStyle: UnorderedListItemStyles, protectedRanges: ProtectedRanges): string {
